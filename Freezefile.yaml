common:
    database: sqlite:///blog.db

exports:

    - query: "SELECT * FROM links"
      filename: links.json
      format: json
      indent: 2

    - query: "SELECT * FROM links"
      filename: links.csv
      format: csv